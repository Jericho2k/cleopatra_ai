-r requirements.txt

modal==1.5.3
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.0
# Applies db/*.sql against a real PostgreSQL in tests/test_*_schema.py.
# Those tests skip when TEST_DATABASE_URL is unset, so local runs need no server.
psycopg[binary]==3.3.2
